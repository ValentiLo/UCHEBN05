# **ПМ05. Тестирование установки интернет-магазина**

## **1. Стратегия тестирования установки**

### **1.1. Матрица тестовых сценариев установки**

```yaml
# installation_test_matrix.yml
installation_scenarios:
  operating_systems:
    - "ubuntu_20.04"
    - "ubuntu_22.04" 
    - "centos_7"
    - "centos_8"
    - "windows_server_2019"
    - "windows_server_2022"
    - "macos_12"
    - "macos_13"

  deployment_methods:
    - "manual_installation"
    - "docker_compose"
    - "docker_swarm"
    - "kubernetes"
    - "cloud_formation"
    - "terraform"

  database_backends:
    - "postgresql_13"
    - "postgresql_14"
    - "postgresql_15"
    - "mysql_8.0"

  environments:
    - "development"
    - "staging"
    - "production"
```

---

## **2. Автоматизированные скрипты тестирования установки**

### **2.1. Главный скрипт тестирования установки**

```python
#!/usr/bin/env python3
# scripts/installation_test_runner.py
import os
import sys
import subprocess
import json
import time
import requests
import socket
from datetime import datetime
from pathlib import Path

class InstallationTester:
    """Класс для тестирования процесса установки"""
    
    def __init__(self):
        self.test_results = {}
        self.start_time = datetime.now()
        
    def run_full_installation_test(self, deployment_method="docker_compose"):
        """Запуск полного теста установки"""
        print("🚀 Starting installation testing...")
        
        test_steps = [
            self.test_system_requirements,
            self.test_dependencies_installation,
            self.test_database_setup,
            self.test_application_installation,
            self.test_service_initialization,
            self.test_health_checks,
            self.test_functionality,
            self.test_performance
        ]
        
        for step in test_steps:
            step_name = step.__name__
            print(f"\n📋 Executing: {step_name}")
            
            try:
                result = step()
                self.test_results[step_name] = {
                    "status": "PASS",
                    "result": result,
                    "timestamp": datetime.now().isoformat()
                }
                print(f"✅ {step_name}: PASSED")
            except Exception as e:
                self.test_results[step_name] = {
                    "status": "FAIL", 
                    "error": str(e),
                    "timestamp": datetime.now().isoformat()
                }
                print(f"❌ {step_name}: FAILED - {str(e)}")
                
        self.generate_installation_report()
        return self.test_results
    
    def test_system_requirements(self):
        """Тестирование системных требований"""
        print("🔍 Checking system requirements...")
        
        requirements = {
            "python_version": self._check_python_version(),
            "memory": self._check_memory(),
            "disk_space": self._check_disk_space(),
            "docker": self._check_docker(),
            "docker_compose": self._check_docker_compose(),
            "ports_availability": self._check_ports_availability()
        }
        
        # Проверяем минимальные требования
        if requirements["memory"]["available_mb"] < 2048:
            raise Exception("Insufficient memory: minimum 2GB required")
            
        if requirements["disk_space"]["free_gb"] < 10:
            raise Exception("Insufficient disk space: minimum 10GB required")
            
        return requirements
    
    def _check_python_version(self):
        """Проверка версии Python"""
        version = sys.version_info
        return {
            "version": f"{version.major}.{version.minor}.{version.micro}",
            "meets_requirement": version >= (3, 8)
        }
    
    def _check_memory(self):
        """Проверка памяти"""
        try:
            import psutil
            memory = psutil.virtual_memory()
            return {
                "total_mb": round(memory.total / 1024 / 1024),
                "available_mb": round(memory.available / 1024 / 1024),
                "percent_used": memory.percent
            }
        except ImportError:
            return {"available_mb": 0, "error": "psutil not available"}
    
    def _check_disk_space(self):
        """Проверка места на диске"""
        try:
            import psutil
            disk = psutil.disk_usage('/')
            return {
                "total_gb": round(disk.total / 1024 / 1024 / 1024, 1),
                "free_gb": round(disk.free / 1024 / 1024 / 1024, 1),
                "percent_used": disk.percent
            }
        except ImportError:
            return {"free_gb": 0, "error": "psutil not available"}
    
    def _check_docker(self):
        """Проверка установки Docker"""
        try:
            result = subprocess.run(
                ["docker", "--version"],
                capture_output=True,
                text=True,
                timeout=10
            )
            return {
                "installed": result.returncode == 0,
                "version": result.stdout.strip() if result.returncode == 0 else "Not installed"
            }
        except (subprocess.TimeoutExpired, FileNotFoundError):
            return {"installed": False, "version": "Not installed"}
    
    def _check_ports_availability(self):
        """Проверка доступности портов"""
        ports_to_check = [8000, 5432, 6379, 8080]
        available_ports = []
        occupied_ports = []
        
        for port in ports_to_check:
            if self._is_port_available(port):
                available_ports.append(port)
            else:
                occupied_ports.append(port)
                
        return {
            "available_ports": available_ports,
            "occupied_ports": occupied_ports
        }
    
    def _is_port_available(self, port):
        """Проверка доступности порта"""
        with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:
            try:
                s.bind(('localhost', port))
                return True
            except socket.error:
                return False
```

### **2.2. Тестирование зависимостей**

```python
# scripts/installation_test_runner.py (продолжение)
    def test_dependencies_installation(self):
        """Тестирование установки зависимостей"""
        print("📦 Testing dependencies installation...")
        
        # Создаем временный requirements файл для тестирования
        test_requirements = """
Django==4.2.7
djangorestframework==3.14.0
psycopg2-binary==2.9.7
redis==4.5.5
celery==5.3.1
pillow==10.0.0
gunicorn==21.2.0
        """
        
        with open("test_requirements.txt", "w") as f:
            f.write(test_requirements)
        
        # Устанавливаем зависимости
        try:
            result = subprocess.run([
                "pip", "install", "-r", "test_requirements.txt"
            ], capture_output=True, text=True, timeout=300)
            
            if result.returncode != 0:
                raise Exception(f"Dependency installation failed: {result.stderr}")
            
            # Проверяем, что все пакеты установились
            installed_packages = self._get_installed_packages()
            required_packages = ["Django", "djangorestframework", "psycopg2-binary"]
            
            missing_packages = []
            for package in required_packages:
                if package.lower() not in [pkg.lower() for pkg in installed_packages]:
                    missing_packages.append(package)
            
            if missing_packages:
                raise Exception(f"Missing packages: {missing_packages}")
            
            return {
                "installed_packages": installed_packages,
                "installation_log": result.stdout
            }
            
        finally:
            # Очистка
            if os.path.exists("test_requirements.txt"):
                os.remove("test_requirements.txt")
    
    def _get_installed_packages(self):
        """Получение списка установленных пакетов"""
        result = subprocess.run([
            "pip", "freeze"
        ], capture_output=True, text=True)
        
        if result.returncode == 0:
            return [line.split('==')[0] for line in result.stdout.strip().split('\n')]
        return []
```

### **2.3. Тестирование установки базы данных**

```python
# scripts/installation_test_runner.py (продолжение)
    def test_database_setup(self):
        """Тестирование настройки базы данных"""
        print("🗄️ Testing database setup...")
        
        # Тестируем подключение к PostgreSQL
        db_checks = {
            "postgresql_connection": self._test_postgresql_connection(),
            "database_creation": self._test_database_creation(),
            "migrations": self._test_database_migrations(),
            "test_data": self._test_test_data_loading()
        }
        
        return db_checks
    
    def _test_postgresql_connection(self):
        """Тестирование подключения к PostgreSQL"""
        try:
            import psycopg2
            conn = psycopg2.connect(
                host="localhost",
                port=5432,
                user="test_user",
                password="test_password",
                database="postgres"
            )
            conn.close()
            return {"status": "SUCCESS", "message": "PostgreSQL connection successful"}
        except Exception as e:
            return {"status": "FAILED", "error": str(e)}
    
    def _test_database_creation(self):
        """Тестирование создания базы данных"""
        try:
            # Создаем тестовую базу данных
            subprocess.run([
                "createdb", "-h", "localhost", "-U", "test_user", "ecommerce_test"
            ], check=True, timeout=30)
            
            return {"status": "SUCCESS", "message": "Database creation successful"}
        except subprocess.CalledProcessError as e:
            return {"status": "FAILED", "error": str(e)}
    
    def _test_database_migrations(self):
        """Тестирование применения миграций"""
        try:
            # Настраиваем переменные окружения для тестовой БД
            env = os.environ.copy()
            env['DATABASE_URL'] = 'postgresql://test_user:test_password@localhost:5432/ecommerce_test'
            
            result = subprocess.run([
                "python", "manage.py", "migrate", "--no-input"
            ], env=env, capture_output=True, text=True, timeout=120)
            
            if result.returncode != 0:
                raise Exception(f"Migrations failed: {result.stderr}")
            
            return {"status": "SUCCESS", "message": "Migrations applied successfully"}
        except Exception as e:
            return {"status": "FAILED", "error": str(e)}
```

### **2.4. Тестирование установки приложения**

```python
# scripts/installation_test_runner.py (продолжение)
    def test_application_installation(self):
        """Тестирование установки приложения"""
        print("🖥️ Testing application installation...")
        
        installation_checks = {
            "static_files": self._test_static_files_collection(),
            "media_directory": self._test_media_directory(),
            "environment_config": self._test_environment_config(),
            "secret_key": self._test_secret_key_generation()
        }
        
        return installation_checks
    
    def _test_static_files_collection(self):
        """Тестирование сбора статических файлов"""
        try:
            env = os.environ.copy()
            env['DJANGO_SETTINGS_MODULE'] = 'config.settings.test'
            
            result = subprocess.run([
                "python", "manage.py", "collectstatic", "--no-input"
            ], env=env, capture_output=True, text=True, timeout=60)
            
            if result.returncode != 0:
                raise Exception(f"Static files collection failed: {result.stderr}")
            
            # Проверяем, что статические файлы созданы
            static_dir = Path("staticfiles")
            if not static_dir.exists():
                raise Exception("Static files directory not created")
            
            css_files = list(static_dir.rglob("*.css"))
            js_files = list(static_dir.rglob("*.js"))
            
            return {
                "status": "SUCCESS",
                "css_files": len(css_files),
                "js_files": len(js_files),
                "total_files": len(list(static_dir.rglob("*")))
            }
        except Exception as e:
            return {"status": "FAILED", "error": str(e)}
    
    def _test_media_directory(self):
        """Тестирование создания медиа директории"""
        try:
            media_dir = Path("media")
            media_dir.mkdir(exist_ok=True)
            
            # Проверяем права на запись
            test_file = media_dir / "test_write.txt"
            test_file.write_text("test")
            test_file.unlink()
            
            return {
                "status": "SUCCESS",
                "writable": True,
                "path": str(media_dir.absolute())
            }
        except Exception as e:
            return {"status": "FAILED", "error": str(e)}
    
    def _test_secret_key_generation(self):
        """Тестирование генерации секретного ключа"""
        try:
            from django.core.management.utils import get_random_secret_key
            secret_key = get_random_secret_key()
            
            if len(secret_key) < 50:
                raise Exception("Secret key too short")
            
            return {
                "status": "SUCCESS",
                "key_length": len(secret_key),
                "is_secure": len(set(secret_key)) > 30  # Проверка энтропии
            }
        except Exception as e:
            return {"status": "FAILED", "error": str(e)}
```

### **2.5. Тестирование сервисов**

```python
# scripts/installation_test_runner.py (продолжение)
    def test_service_initialization(self):
        """Тестирование инициализации сервисов"""
        print("🔧 Testing service initialization...")
        
        services = {
            "web_server": self._test_web_server(),
            "celery_worker": self._test_celery_worker(),
            "celery_beat": self._test_celery_beat(),
            "redis_cache": self._test_redis_cache()
        }
        
        return services
    
    def _test_web_server(self):
        """Тестирование веб-сервера"""
        try:
            # Запускаем сервер в фоновом режиме
            env = os.environ.copy()
            env['DJANGO_SETTINGS_MODULE'] = 'config.settings.test'
            
            server_process = subprocess.Popen([
                "python", "manage.py", "runserver", "8000"
            ], env=env, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
            
            # Даем серверу время на запуск
            time.sleep(5)
            
            # Проверяем доступность
            try:
                response = requests.get("http://localhost:8000/", timeout=10)
                status_ok = response.status_code == 200
            except:
                status_ok = False
            
            # Завершаем процесс
            server_process.terminate()
            server_process.wait(timeout=10)
            
            return {
                "status": "SUCCESS" if status_ok else "FAILED",
                "accessible": status_ok,
                "process_id": server_process.pid
            }
            
        except Exception as e:
            return {"status": "FAILED", "error": str(e)}
    
    def _test_celery_worker(self):
        """Тестирование Celery worker"""
        try:
            env = os.environ.copy()
            env['DJANGO_SETTINGS_MODULE'] = 'config.settings.test'
            
            worker_process = subprocess.Popen([
                "celery", "-A", "config", "worker", "--loglevel=info", "--concurrency=1"
            ], env=env, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
            
            time.sleep(3)
            
            # Проверяем, что процесс работает
            if worker_process.poll() is None:
                worker_process.terminate()
                worker_process.wait(timeout=5)
                return {"status": "SUCCESS", "message": "Celery worker started successfully"}
            else:
                return {"status": "FAILED", "error": "Celery worker failed to start"}
                
        except Exception as e:
            return {"status": "FAILED", "error": str(e)}
```

### **2.6. Функциональное тестирование после установки**

```python
# scripts/installation_test_runner.py (продолжение)
    def test_health_checks(self):
        """Тестирование health checks"""
        print("❤️ Testing health checks...")
        
        health_checks = {
            "database_health": self._check_database_health(),
            "cache_health": self._check_cache_health(),
            "storage_health": self._check_storage_health(),
            "api_health": self._check_api_health()
        }
        
        return health_checks
    
    def _check_database_health(self):
        """Проверка здоровья базы данных"""
        try:
            from django.db import connection
            with connection.cursor() as cursor:
                cursor.execute("SELECT 1")
                result = cursor.fetchone()
            
            return {
                "status": "HEALTHY" if result[0] == 1 else "UNHEALTHY",
                "response_time_ms": 0  # Можно добавить измерение времени
            }
        except Exception as e:
            return {"status": "UNHEALTHY", "error": str(e)}
    
    def test_functionality(self):
        """Тестирование базовой функциональности после установки"""
        print("🔍 Testing basic functionality...")
        
        functionality_checks = {
            "admin_interface": self._test_admin_interface(),
            "api_endpoints": self._test_api_endpoints(),
            "product_management": self._test_product_management(),
            "user_authentication": self._test_user_authentication()
        }
        
        return functionality_checks
    
    def _test_admin_interface(self):
        """Тестирование админ-интерфейса"""
        try:
            # Создаем тестового суперпользователя
            env = os.environ.copy()
            env['DJANGO_SETTINGS_MODULE'] = 'config.settings.test'
            
            result = subprocess.run([
                "python", "manage.py", "createsuperuser",
                "--no-input",
                "--email", "admin@test.com",
                "--username", "admin"
            ], env=env, capture_output=True, text=True, input="testpassword123\n")
            
            return {
                "status": "SUCCESS",
                "superuser_created": result.returncode == 0
            }
        except Exception as e:
            return {"status": "FAILED", "error": str(e)}
    
    def _test_api_endpoints(self):
        """Тестирование API endpoints"""
        endpoints_to_test = [
            "/api/products/",
            "/api/categories/",
            "/api/auth/login/",
            "/api/health/"
        ]
        
        results = {}
        for endpoint in endpoints_to_test:
            try:
                response = requests.get(f"http://localhost:8000{endpoint}", timeout=5)
                results[endpoint] = {
                    "status_code": response.status_code,
                    "response_time_ms": response.elapsed.total_seconds() * 1000
                }
            except Exception as e:
                results[endpoint] = {
                    "status_code": 0,
                    "error": str(e)
                }
        
        return results
```

### **2.7. Генерация отчетов**

```python
# scripts/installation_test_runner.py (продолжение)
    def generate_installation_report(self):
        """Генерация отчета об установке"""
        print("\n📊 Generating installation report...")
        
        end_time = datetime.now()
        duration = (end_time - self.start_time).total_seconds()
        
        report = {
            "installation_test_report": {
                "start_time": self.start_time.isoformat(),
                "end_time": end_time.isoformat(),
                "duration_seconds": duration,
                "total_tests": len(self.test_results),
                "passed_tests": sum(1 for r in self.test_results.values() if r["status"] == "PASS"),
                "failed_tests": sum(1 for r in self.test_results.values() if r["status"] == "FAIL"),
                "success_rate": (sum(1 for r in self.test_results.values() if r["status"] == "PASS") / len(self.test_results)) * 100
            },
            "test_results": self.test_results,
            "system_info": self._get_system_info()
        }
        
        # Сохраняем отчет
        os.makedirs("installation_reports", exist_ok=True)
        report_file = f"installation_reports/installation_test_{self.start_time.strftime('%Y%m%d_%H%M%S')}.json"
        
        with open(report_file, 'w') as f:
            json.dump(report, f, indent=2, ensure_ascii=False)
        
        # Генерируем человеко-читаемый отчет
        self._generate_human_readable_report(report, report_file.replace('.json', '.txt'))
        
        print(f"✅ Installation test completed in {duration:.2f} seconds")
        print(f"📄 Full report saved to: {report_file}")
        
        return report
    
    def _get_system_info(self):
        """Получение информации о системе"""
        import platform
        
        return {
            "platform": platform.platform(),
            "system": platform.system(),
            "release": platform.release(),
            "architecture": platform.architecture()[0],
            "processor": platform.processor(),
            "python_version": platform.python_version()
        }
    
    def _generate_human_readable_report(self, report, filename):
        """Генерация человеко-читаемого отчета"""
        with open(filename, 'w') as f:
            f.write("=" * 60 + "\n")
            f.write("INSTALLATION TEST REPORT\n")
            f.write("=" * 60 + "\n\n")
            
            f.write(f"Test Date: {report['installation_test_report']['start_time']}\n")
            f.write(f"Duration: {report['installation_test_report']['duration_seconds']:.2f} seconds\n")
            f.write(f"Success Rate: {report['installation_test_report']['success_rate']:.1f}%\n\n")
            
            f.write("SYSTEM INFORMATION:\n")
            for key, value in report['system_info'].items():
                f.write(f"  {key}: {value}\n")
            
            f.write("\nTEST RESULTS:\n")
            for test_name, result in report['test_results'].items():
                status_icon = "✅" if result["status"] == "PASS" else "❌"
                f.write(f"{status_icon} {test_name}: {result['status']}\n")
            
            f.write("\nDETAILED RESULTS:\n")
            for test_name, result in report['test_results'].items():
                f.write(f"\n{test_name}:\n")
                f.write(f"  Status: {result['status']}\n")
                f.write(f"  Timestamp: {result['timestamp']}\n")
                
                if "error" in result:
                    f.write(f"  Error: {result['error']}\n")
                elif "result" in result:
                    f.write(f"  Result: {json.dumps(result['result'], indent=2)}\n")

if __name__ == "__main__":
    tester = InstallationTester()
    
    if len(sys.argv) > 1:
        deployment_method = sys.argv[1]
    else:
        deployment_method = "docker_compose"
    
    results = tester.run_full_installation_test(deployment_method)
    
    # Возвращаем код выхода в зависимости от результатов
    success_rate = results['installation_test_report']['success_rate']
    sys.exit(0 if success_rate >= 80 else 1)
```

---

## **3. Docker-based тестирование установки**

### **3.1. Dockerfile для тестирования установки**

```dockerfile
# Dockerfile.installation-test
FROM python:3.11-slim

# Установка системных зависимостей
RUN apt-get update && apt-get install -y \
    postgresql-client \
    redis-tools \
    curl \
    wget \
    && rm -rf /var/lib/apt/lists/*

# Создание рабочей директории
WORKDIR /app

# Копирование requirements
COPY requirements.txt .
COPY requirements-test.txt .

# Установка Python зависимостей
RUN pip install --no-cache-dir -r requirements.txt
RUN pip install --no-cache-dir -r requirements-test.txt

# Копирование исходного кода
COPY . .

# Копирование скриптов тестирования
COPY scripts/installation_test_runner.py /scripts/
COPY scripts/health_checker.py /scripts/

# Создание директорий
RUN mkdir -p /app/staticfiles /app/media /app/logs

# Настройка прав
RUN chmod +x /scripts/*.py

# Команда для запуска тестов установки
CMD ["python", "/scripts/installation_test_runner.py"]
```

### **3.2. Docker Compose для тестирования установки**

```yaml
# docker-compose.installation-test.yml
version: '3.8'

services:
  # База данных для тестирования
  test-database:
    image: postgres:14
    environment:
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: test_password
      POSTGRES_DB: ecommerce_test
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U test_user -d ecommerce_test"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Redis для тестирования
  test-redis:
    image: redis:7-alpine
    ports:
      - "6380:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # Приложение для тестирования установки
  installation-tester:
    build:
      context: .
      dockerfile: Dockerfile.installation-test
    environment:
      DATABASE_URL: postgresql://test_user:test_password@test-database:5432/ecommerce_test
      REDIS_URL: redis://test-redis:6379/0
      DJANGO_SETTINGS_MODULE: config.settings.test
    depends_on:
      test-database:
        condition: service_healthy
      test-redis:
        condition: service_healthy
    volumes:
      - ./installation_reports:/app/installation_reports
      - ./logs:/app/logs

  # Health checker
  health-checker:
    image: curlimages/curl:latest
    depends_on:
      - installation-tester
    command: >
      sh -c "
        echo 'Waiting for installation tests to complete...'
        while ! curl -f http://installation-tester:8000/health/ > /dev/null 2>&1; do
          sleep 5
        done
        echo 'Installation tests completed successfully!'
      "
```

---

## **4. Интеграция с CI/CD**

### **4.1. GitHub Actions workflow для тестирования установки**

```yaml
# .github/workflows/installation-test.yml
name: Installation Testing

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 2 * * 1'  # Каждый понедельник в 2:00

jobs:
  installation-test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-20.04, ubuntu-22.04, windows-latest, macos-latest]
        python-version: ['3.8', '3.9', '3.10', '3.11']
    
    services:
      postgres:
        image: postgres:14
        env:
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
          POSTGRES_DB: ecommerce_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
      
      redis:
        image: redis:7-alpine
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379

    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Install system dependencies
      run: |
        if [ "$RUNNER_OS" == "Linux" ]; then
          sudo apt-get update
          sudo apt-get install -y postgresql-client redis-tools
        fi
    
    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-test.txt
    
    - name: Run installation tests
      env:
        DATABASE_URL: postgresql://test_user:test_password@localhost:5432/ecommerce_test
        REDIS_URL: redis://localhost:6379/0
        DJANGO_SETTINGS_MODULE: config.settings.test
      run: |
        python scripts/installation_test_runner.py docker_compose
    
    - name: Upload installation reports
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: installation-reports-${{ matrix.os }}-py${{ matrix.python-version }}
        path: installation_reports/
        retention-days: 30
    
    - name: Check installation success
      run: |
        success_rate=$(python -c "
        import json, glob
        reports = glob.glob('installation_reports/*.json')
        if reports:
            with open(reports[-1]) as f:
                data = json.load(f)
            print(data['installation_test_report']['success_rate'])
        else:
            print(0)
        ")
        if (( $(echo "$success_rate < 80" | bc -l) )); then
          echo '❌ Installation test failed'
          exit 1
        else
          echo '✅ Installation test passed'
        fi
```

---

## **5. Утилиты для ручного тестирования установки**

### **5.1. Скрипт проверки окружения**

```bash
#!/bin/bash
# scripts/check_environment.sh

echo "🔍 Checking installation environment..."

# Проверка Python
echo "Python:"
python --version
pip --version

# Проверка Docker
echo -e "\nDocker:"
docker --version
docker-compose --version

# Проверка портов
echo -e "\nPorts:"
for port in 8000 5432 6379; do
    if lsof -Pi :$port -sTCP:LISTEN -t >/dev/null ; then
        echo "Port $port: OCCUPIED"
    else
        echo "Port $port: FREE"
    fi
done

# Проверка дискового пространства
echo -e "\nDisk space:"
df -h .

# Проверка памяти
echo -e "\nMemory:"
free -h

echo -e "\n✅ Environment check completed"
```

### **5.2. Скрипт пошаговой установки**

```python
#!/usr/bin/env python3
# scripts/interactive_installer.py

import os
import sys
import subprocess
import getpass
from pathlib import Path

class InteractiveInstaller:
    """Интерактивный установщик с проверками"""
    
    def run_installation(self):
        """Запуск интерактивной установки"""
        print("🎯 E-Commerce Platform Interactive Installer")
        print("=" * 50)
        
        steps = [
            self.check_prerequisites,
            self.configure_environment,
            self.setup_database,
            self.install_dependencies,
            self.run_migrations,
            self.collect_static,
            self.create_superuser,
            self.start_services,
            self.verify_installation
        ]
        
        for step in steps:
            if not step():
                print(f"❌ Installation failed at step: {step.__name__}")
                return False
        
        print("✅ Installation completed successfully!")
        return True
    
    def check_prerequisites(self):
        """Проверка предварительных требований"""
        print("\n1. Checking prerequisites...")
        
        checks = [
            ("Python 3.8+", self._check_python_version),
            ("Pip", self._check_pip),
            ("Database", self._check_database_connection),
            ("Redis", self._check_redis),
        ]
        
        all_passed = True
        for check_name, check_func in checks:
            try:
                result = check_func()
                status = "✅" if result else "❌"
                print(f"   {status} {check_name}")
                if not result:
                    all_passed = False
            except Exception as e:
                print(f"   ❌ {check_name}: {e}")
                all_passed = False
        
        return all_passed
    
    def _check_python_version(self):
        """Проверка версии Python"""
        return sys.version_info >= (3, 8)
    
    def _check_database_connection(self):
        """Проверка подключения к базе данных"""
        try:
            # Попытка подключения к PostgreSQL
            subprocess.run([
                "psql", "--version"
            ], capture_output=True, check=True)
            return True
        except:
            return False

if __name__ == "__main__":
    installer = InteractiveInstaller()
    success = installer.run_installation()
    sys.exit(0 if success else 1)
```

---

## **6. Чек-лист тестирования установки**

```markdown
# Чек-лист тестирования установки

## ✅ Предварительные требования
- [ ] Python 3.8+ установлен
- [ ] Pip обновлен до последней версии
- [ ] PostgreSQL 13+ доступен
- [ ] Redis 6+ доступен
- [ ] Достаточно места на диске (>10GB)
- [ ] Достаточно памяти (>2GB RAM)
- [ ] Порты 8000, 5432, 6379 свободны

## 📦 Установка зависимостей
- [ ] Requirements установлены без ошибок
- [ ] Все пакеты совместимы
- [ ] Отсутствуют конфликты версий

## 🗄️ База данных
- [ ] Подключение к PostgreSQL успешно
- [ ] База данных создана
- [ ] Миграции применены
- [ ] Тестовые данные загружены

## 🖥️ Приложение
- [ ] Статические файлы собраны
- [ ] Медиа-директория создана
- [ ] Переменные окружения настроены
- [ ] Секретный ключ сгенерирован

## 🔧 Сервисы
- [ ] Веб-сервер запускается
- [ ] Celery worker работает
- [ ] Celery beat планировщик активен
- [ ] Redis кэш доступен

## ❤️ Health Checks
- [ ] База данных отвечает
- [ ] Кэш работает
- [ ] Файловая система доступна
- [ ] API endpoints отвечают

## 🚀 Функциональность
- [ ] Админ-панель доступна
- [ ] API endpoints работают
- [ ] Аутентификация функционирует
- [ ] Управление товарами работает
```

Данная система тестирования установки обеспечивает:

1. **Автоматизированную проверку** всех этапов установки
2. **Кросс-платформенное тестирование** на разных ОС
3. **Детальную отчетность** о проблемах установки
4. **Интеграцию с CI/CD** для непрерывного контроля
5. **Интерактивные инструменты** для ручной установки

Это гарантирует надежную и предсказуемую установку интернет-магазина в любой среде.
