Вот пример задания для государственного экзамена по специальности 09.02.07 "Информационные системы и программирование":

# **ГОСУДАРСТВЕННЫЙ ЭКЗАМЕН**
**Специальность:** 09.02.07 "Информационные системы и программирование"
**Квалификация:** Программист

---

## **ВАРИАНТ 1**

### **Раздел 1. Теоретические вопросы**

**1. Архитектура информационных систем**
- Дайте определение понятию "информационная система". Перечислите основные компоненты ИС.
- Опишите отличия двухзвенной и трехзвенной архитектуры клиент-серверных приложений. Приведите примеры использования каждой архитектуры.

**2. Базы данных**
- Опишите основные типы связей между таблицами в реляционных базах данных (1:1, 1:M, M:M). Приведите примеры для каждого типа.
- Напишите SQL-запросы для следующих операций:
  а) Создание таблицы "Студенты" с полями: id, ФИО, дата_рождения, группа_id
  б) Выбор всех студентов из группы "ПИ-21"
  в) Обновление данных о студенте

**3. Технологии программирования**
- Сравните принципы объектно-ориентированного программирования (ООП) и процедурного программирования. Назовите четыре основных принципа ООП.
- Объясните разницу между статическим и динамическим связыванием в программировании.

---

### **Раздел 2. Практическое задание**

**Задание:** Разработать консольное приложение "Учет книг в библиотеке" на языке Python.

**Требования к приложению:**

**1. Функциональные требования:**
- Хранение данных о книгах (автор, название, год издания, ISBN, статус "выдана"/"в наличии")
- Хранение данных о читателях (ФИО, номер читательского билета, контакты)
- Учет выдачи и возврата книг
- Поиск книг по различным критериям
- Формирование отчетов

**2. Требования к реализации:**
- Использовать объектно-ориентированный подход
- Реализовать работу с базой данных SQLite
- Реализовать модульную архитектуру
- Обеспечить обработку исключительных ситуаций

**3. Минимальный набор модулей:**
- `main.py` - основной модуль
- `database.py` - работа с базой данных
- `models.py` - классы сущностей
- `ui.py` - пользовательский интерфейс

**4. Основные функции меню:**
```
1. Добавить книгу
2. Удалить книгу
3. Поиск книги
4. Показать все книги
5. Добавить читателя
6. Выдать книгу
7. Принять книгу
8. Показать задолженности
9. Выход
```

**5. Схема базы данных:**

```sql
CREATE TABLE books (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    author TEXT NOT NULL,
    title TEXT NOT NULL,
    year INTEGER,
    isbn TEXT UNIQUE,
    status TEXT DEFAULT 'available'
);

CREATE TABLE readers (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    full_name TEXT NOT NULL,
    card_number TEXT UNIQUE NOT NULL,
    phone TEXT
);

CREATE TABLE transactions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    book_id INTEGER,
    reader_id INTEGER,
    issue_date DATE,
    return_date DATE,
    FOREIGN KEY (book_id) REFERENCES books(id),
    FOREIGN KEY (reader_id) REFERENCES readers(id)
);
```

**6. Примеры тестовых данных:**
```
Книги:
1. Толстой Л.Н. | Война и мир | 1869 | 9785171201557
2. Достоевский Ф.М. | Преступление и наказание | 1866 | 9785171201568

Читатели:
1. Иванов Иван Иванович | ЛБ-001 | +79001234567
2. Петрова Анна Сергеевна | ЛБ-002 | +79007654321
```

---

### **Раздел 3. Документация**

**Требуется предоставить:**

1. **Техническое задание** с описанием функциональных требований
2. **Диаграмму классов** UML (не менее 3 классов с атрибутами и методами)
3. **Инструкцию пользователя**
4. **Инструкцию по установке и запуску**

---

### **Критерии оценки:**

**"Отлично" (5 баллов):**
- Полное выполнение всех требований
- Корректная работа всех функций
- Грамотно спроектированная архитектура
- Полная документация
- Наличие обработки исключений

**"Хорошо" (4 балла):**
- Выполнены основные требования
- Незначительные недочеты в реализации
- Неполная документация

**"Удовлетворительно" (3 балла):**
- Выполнены базовые требования
- Присутствуют ошибки в работе
- Минимальная документация

**"Неудовлетворительно" (2 балла):**
- Не выполнены основные требования
- Критические ошибки в работе
- Отсутствие документации

---

## **ВАРИАНТ 2**

### **Раздел 1. Теоретические вопросы**

**1. Веб-технологии**
- Опишите модель взаимодействия клиент-сервер в веб-приложениях. Что такое HTTP-протокол?
- Объясните разницу между Cookie и Session. В каких случаях используются?

**2. Сетевое программирование**
- Что такое сокет (socket) в программировании? Опишите основные этапы создания TCP-соединения.
- Объясните понятие "многопоточность" и ее применение в сетевых приложениях.

**3. Безопасность информационных систем**
- Перечислите основные виды угроз информационной безопасности.
- Опишите методы защиты данных в базах данных.

---

### **Раздел 2. Практическое задание**

**Задание:** Разработать веб-приложение "Интернет-магазин электроники" с использованием Flask.

**Требования:**

**1. Функциональные требования:**
- Каталог товаров с категориями
- Корзина покупок
- Регистрация и авторизация пользователей
- Панель администратора для управления товарами
- Поиск товаров
- Оформление заказа

**2. Технологический стек:**
- Backend: Flask
- База данных: SQLite/PostgreSQL
- Frontend: HTML, CSS, Jinja2
- Аутентификация: Flask-Login

**3. Основные маршруты (routes):**
- `/` - главная страница с товарами
- `/category/<id>` - товары категории
- `/product/<id>` - страница товара
- `/cart` - корзина
- `/checkout` - оформление заказа
- `/login`, `/register` - авторизация
- `/admin` - панель администратора

**4. Модели базы данных:**
```python
class User(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    username = db.Column(db.String(80), unique=True)
    email = db.Column(db.String(120), unique=True)
    password_hash = db.Column(db.String(128))
    is_admin = db.Column(db.Boolean, default=False)

class Product(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    name = db.Column(db.String(100))
    description = db.Column(db.Text)
    price = db.Column(db.Float)
    category_id = db.Column(db.Integer, db.ForeignKey('category.id'))
    image = db.Column(db.String(200))

class Category(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    name = db.Column(db.String(50))

class Order(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    user_id = db.Column(db.Integer, db.ForeignKey('user.id'))
    total = db.Column(db.Float)
    status = db.Column(db.String(20))
    created_at = db.Column(db.DateTime, default=datetime.utcnow)
```

---

### **Дополнительные задания для повышенной сложности:**

**1. Для консольного приложения:**
- Реализовать графический интерфейс с использованием Tkinter
- Добавить экспорт отчетов в Excel/PDF
- Реализовать многопользовательский режим

**2. Для веб-приложения:**
- Добавить REST API
- Реализовать пагинацию товаров
- Добавить систему отзывов и рейтингов
- Интегрировать оплату через платежные системы

---

## **Типовой пример решения (частичный) для Варианта 1:**

```python
# models.py
class Book:
    def __init__(self, author, title, year, isbn, status='available'):
        self.author = author
        self.title = title
        self.year = year
        self.isbn = isbn
        self.status = status
    
    def __str__(self):
        return f"{self.author} - {self.title} ({self.year}) - {self.status}"

# database.py
import sqlite3

class Database:
    def __init__(self, db_name='library.db'):
        self.connection = sqlite3.connect(db_name)
        self.cursor = self.connection.cursor()
        self.create_tables()
    
    def create_tables(self):
        self.cursor.execute('''
            CREATE TABLE IF NOT EXISTS books (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                author TEXT NOT NULL,
                title TEXT NOT NULL,
                year INTEGER,
                isbn TEXT UNIQUE,
                status TEXT DEFAULT 'available'
            )
        ''')
        self.connection.commit()

# main.py
from database import Database
from models import Book

class LibraryApp:
    def __init__(self):
        self.db = Database()
    
    def show_menu(self):
        print("\n=== БИБЛИОТЕЧНАЯ СИСТЕМА ===")
        print("1. Добавить книгу")
        print("2. Показать все книги")
        print("3. Поиск книги")
        print("4. Выйти")
    
    def run(self):
        while True:
            self.show_menu()
            choice = input("\nВыберите действие: ")
            
            if choice == '1':
                self.add_book()
            elif choice == '2':
                self.show_books()
            elif choice == '3':
                self.search_book()
            elif choice == '4':
                print("Выход из программы")
                break
    
    def add_book(self):
        print("\n=== ДОБАВЛЕНИЕ КНИГИ ===")
        author = input("Автор: ")
        title = input("Название: ")
        year = input("Год издания: ")
        isbn = input("ISBN: ")
        
        try:
            self.db.cursor.execute(
                "INSERT INTO books (author, title, year, isbn) VALUES (?, ?, ?, ?)",
                (author, title, year, isbn)
            )
            self.db.connection.commit()
            print("Книга успешно добавлена!")
        except Exception as e:
            print(f"Ошибка: {e}")

if __name__ == "__main__":
    app = LibraryApp()
    app.run()
```

---

## **Вопросы для защиты:**

1. Обоснуйте выбор архитектуры приложения.
2. Какие методы оптимизации запросов к базе данных вы использовали?
3. Как обеспечивается безопасность данных в вашем приложении?
4. Какие альтернативные решения вы рассматривали?
5. Как можно расширить функционал приложения?

**Время выполнения:** 4 академических часа (180 минут)

**Разрешенные материалы:** документация по языку программирования, IDE, интернет (только для справочной информации).

**Запрещено:** использование готовых решений, копирование кода, общение с другими экзаменуемыми.
